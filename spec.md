# spec.md

# 拾光校园失物招领系统功能规格说明书

## 1. 项目目标

为校园师生提供统一的失物招领信息平台，使用户可以发布、查询和认领物品；使失物招领管理员可以审核和维护信息；使系统管理员可以维护账号、公告和全校统计数据。

## 2. 用户角色

### 2.1 普通学生

- 浏览校园失物和招领信息
- 按关键词、物品类型、分类和地点查找信息
- 查看物品详情
- 发布丢失或捡到物品的信息
- 查看和管理自己的发布记录
- 提交认领申请并查看申请状态

### 2.2 失物招领管理员

- 查看待审核发布信息
- 审核通过发布信息
- 查看全部物品记录
- 维护物品状态
- 处理认领申请

### 2.3 系统管理员

- 查看全校失物招领数据总览
- 查看发布量、归还量、待处理量和归还率
- 管理系统账号及角色
- 管理公告标题、日期、标签和内容

## 3. 功能规格

### 3.1 首页与信息查询

首页展示物品卡片，卡片至少包含：

- 物品名称
- 失物或招领类型
- 物品分类
- 地点
- 发布时间
- 当前状态
- 信息描述
- 发布人

用户可以通过搜索框匹配物品名称、地点和分类，也可以切换“全部、寻物、招领”筛选条件。点击卡片打开详情弹窗。

### 3.2 发布信息

发布表单支持：

- 选择“我丢失了物品”或“我捡到了物品”
- 物品名称
- 物品分类
- 丢失或拾取地点
- 联系方式
- 详细描述
- 物品照片入口

物品名称和地点为必填项。普通学生提交后状态为“待审核”，并记录当前用户为发布人。

### 3.3 物品状态

当前状态包括：

| 状态 | 说明 |
| --- | --- |
| 待审核 | 学生提交后等待管理员审核 |
| 招领中 | 信息审核通过，等待失主或拾主处理 |
| 待认领 | 已有物品信息，等待用户提交申请 |
| 审核中 | 认领申请已经提交，等待管理员处理 |
| 已认领 | 物品已经完成归还 |

核心状态流转：

```text
发布信息 -> 待审核 -> 招领中 -> 认领申请审核中 -> 已认领
```

### 3.4 个人中心

“我的发布”只展示当前用户作为作者的记录；“我的认领”展示当前用户提交的认领申请、申请时间和审核状态。

### 3.5 管理员功能

失物招领管理员页面包含：

- 待处理审核数量
- 本周已处理数量
- 待认领物品数量
- 待审核列表
- 通过审核操作
- 全部物品状态列表

系统管理员页面包含：

- 数据指标卡
- 近 30 日发布量和归还量趋势
- 高频地点排名
- 账号列表
- 公告列表

## 4. 非功能要求

- 页面适配桌面端和移动端。
- 主要操作应有即时反馈提示。
- 组件和状态命名应保持清晰，便于初学者理解和维护。
- 应避免未经授权的角色操作；真实版本必须由后端再次校验权限。
- 生产构建必须通过 `npm run build`。

## 5. 当前实现与验收边界

当前版本是可交互前端原型，已经用 Pinia Mock 数据演示主要业务闭环。真实项目还需要补充：

- 登录、注册、Token 和后端权限校验
- MySQL 等数据库持久化
- REST API 或其他后端接口
- 图片上传和对象存储
- 认领申请的管理员同意、拒绝和完成归还操作
- 分页、表单校验、错误处理和操作日志

## 6. 建议的数据模型

### User

`id`、`name`、`studentNo`、`role`、`status`、`createdAt`

### Item

`id`、`type`、`title`、`category`、`location`、`description`、`images`、`contact`、`status`、`authorId`、`createdAt`

### Claim

`id`、`itemId`、`applicantId`、`proof`、`status`、`reviewerId`、`createdAt`、`updatedAt`

### Notice

`id`、`title`、`content`、`tag`、`publishedBy`、`publishedAt`、`status`

## 7. 登录与路由守卫规格

### 7.1 登录功能

登录页提供校园账号、密码和登录身份选择。当前前端原型使用非空校验和本地 Mock Token 演示登录流程；正式版本应由 Go 后端校验账号密码并返回 JWT 或其他 Token。

登录成功后：

- 保存认证状态和当前角色。
- 跳转到用户原本想访问的页面；没有目标页面时进入工作台。
- 工作台根据角色展示对应菜单。

### 7.2 路由守卫

路由分为公开路由和受保护路由：

- `/login`：公开登录页，已登录用户访问时跳回 `/app`。
- `/app`：受保护的工作台，未登录访问时跳转 `/login?redirect=/app`。

路由守卫只负责前端体验，不能替代后端权限校验。真实接口请求仍需携带 Token，并由 Go 后端检查用户身份和角色。

### 7.3 TypeScript 迁移要求

- 入口、路由、Pinia store 和 Vite 配置使用 TypeScript。
- 物品、认领申请、用户和角色定义为明确类型。
- 使用 `vue-tsc --noEmit` 进行类型检查。
