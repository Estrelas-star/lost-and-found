# 拾光校园失物招领系统功能规格说明书

## 1. 项目目标

为校园师生提供统一的失物招领信息平台，使用户可以发布、查询和认领物品；使失物招领管理员可以审核和维护信息；使系统管理员可以维护账号、公告和全校统计数据。

## 2. 用户角色

### 2.1 普通学生 `student`

- 浏览校园失物和招领信息
- 按关键词、物品类型、分类、地点和时间查找信息
- 查看物品详情，点赞、收藏、评论与楼中楼回复
- 发布丢失或捡到物品的信息（含图片、多标签、层级地点）
- 查看和管理自己的发布记录
- 提交认领申请并查看申请状态

### 2.2 失物招领管理员 `itemAdmin`

- 查看待审核发布信息，通过或驳回（驳回需填写原因）
- 查看并维护全部物品：编辑、下架/上架、删除
- 处理认领相关状态

### 2.3 系统管理员 `systemAdmin`

- 查看全校失物招领数据总览（指标卡、趋势、高频地点）
- 管理系统账号及角色
- 管理公告标题、日期、标签和内容

## 3. 功能规格

### 3.1 首页与信息查询

首页展示物品卡片，卡片包含：物品名称、失物/招领类型、物品状态、地点、发布时间、描述、发布人。

支持的多维筛选：

| 筛选维度 | 可选值 |
| --- | --- |
| 关键词 | 匹配名称、地点、标签 |
| 类型 | 全部 / 寻物 `lost` / 招领 `found` |
| 分类 | 全部 / 数码 / 证件 / 日用 / 服饰 / 书籍 / 其他 |
| 地点 | 全部 / 从现有物品地点首段去重生成 |
| 时间 | 全部 / 近 3 天 / 近 7 天 / 近 30 天 |

列表支持分页（默认每页 6 条，可选 12/18）。

### 3.2 信息发布（PublishForm）

发布表单字段与校验规则：

| 字段 | 控件 | 校验规则 |
| --- | --- | --- |
| 信息类型 | radio-button（丢失 `lost` / 拾取 `found`） | 必选（默认 lost） |
| 物品名称 | `el-input` | 必填，至少 2 个字符 |
| 物品标签 | `el-select multiple` | **必选至少 1 个**（多标签多选，从 6 类中多选） |
| 丢失/拾取地点 | 动态地点联动选择器（见下） | 校区、建筑/地点必选；**动态出现的必填字段**（楼栋/楼层/详细地址）都必须满足 |
| 联系方式 | `el-input` | 必填，至少 5 个字符 |
| 详细描述 | `el-textarea` | 必填，至少 10 个字符 |
| 物品图片 | 自定义上传 | **最多 3 张，仅图片格式** |

#### 地点选择与数据结构（动态地点联动选择器）

##### 固定学校

- 学校固定为 **浙江工业大学（Zhejiang University of Technology）**，不提供学校选择项。

##### 层级逻辑

```text
校区 -> 建筑/地点 ->（按建筑类型动态）楼栋 / 楼层 / 详细地址
```

- 第一级：**校区**（朝晖、屏峰、莫干山）。
- 第二级：**建筑/地点**（宿舍楼、教学楼、食堂、图书馆、精弘桥周边、操场、绿地广场等）。

##### 动态规则（核心）

根据第二级所选「建筑/地点」的类型，动态决定后续字段：

| 建筑/地点类型 | 动态展示的字段 |
| --- | --- |
| 宿舍楼 | 【第几栋】下拉框 + 【几楼】下拉框 |
| 教学楼 / 食堂 / 图书馆 | 仅【几楼】下拉框 |
| 精弘桥周边 / 操场 / 绿地广场（平面地标） | 不显示楼栋/楼层，直接显示【请输入详细地址】文本框 |

##### 地址拼接

「已选择地点」自动拼接为 **校区 · 建筑/地点 · 楼栋 · 楼层 · 详细地址**，并同步写入 `form.location`。例如：

- `朝晖校区 · 宿舍楼 · 3栋 · 2楼 · 靠窗自习室`
- `朝晖校区 · 精弘桥周边 · 桥头左侧`

任一级数据变化都会重新拼接；去掉某一级时，右侧自动回退到剩余已选层级。

##### 校验规则

所有动态出现的必填字段必须触发表单校验，未填不可提交：

- 必须选择校区；
- 必须选择建筑/地点；
- 选了「宿舍楼」：必须选择楼栋，且必须选择楼层；
- 选了「教学楼 / 食堂 / 图书馆」：必须选择楼层；
- 选了平面地标（精弘桥/操场等）：必须填写详细地址文本框；
- 宿舍楼等场景在选完楼栋/楼层后，仍需填写详细地址。

#### 图片上传限制

- 仅接受图片类型（`image/*`）；
- **最多 3 张**，超出时截断并提示「最多只能上传 3 张图片」；
- 本地预览（`el-image` 支持放大），支持删除并释放本地 Object URL；
- 实时校验：图片数量超过 3 张时报错。

### 3.3 物品状态与状态流转

#### 状态定义

| 状态 | 说明 |
| --- | --- |
| 待审核 | 学生提交后等待管理员审核 |
| 招领中 | 信息审核通过，处于可被认领的阶段 |
| 待认领 | 已有可被认领的信息（原型中与招领中并列可用） |
| 已认领 | 物品已完成归还 |
| 已驳回 | 管理员驳回，需填写原因 |
| 已关闭 | 被下架（管理员可在管理页下架/上架） |

#### 核心状态流转

```text
发布信息
   │
   ├──(管理员通过)──> 招领中 ──> 已认领
   └──(管理员驳回)──> 已驳回

招领中 / 待认领 ──(管理员下架)──> 已关闭
已关闭 ─────────────(管理员上架)──> 招领中
```

#### 申请认领的可用状态

只有「招领中」和「待认领」状态才可申请认领；「已认领」「已关闭」「已驳回」「待审核」状态不可申请（点击时提示「该物品当前不可申请认领」）。
### 3.4 详情弹窗交互规范（DetailDialog）

- 使用 `el-dialog`，宽度适配 `min(620px, 94vw)`。
- **内部滚动**：正文包裹在 `.detail-dialog-scroll` 容器内，超出设定高度时容器内部滚动，避免整个页面被顶出。
- 图片区：`el-carousel` 轮播（高 250px，可缩放预览）；无图时显示占位图形与「暂无照片」。
- 详情区：寻物/招领标识 + 日期、标题、多标签、描述、地点、发布时间与发布人。
- 统计区：浏览、点赞、收藏，可即时切换。
- 评论区：
  - 顶部显示评论总数；
  - 支持发表评论（textarea，`maxlength=200`），支持 `@` 楼中楼回复与取消回复；
  - 支持给每条评论点赞；无评论时显示「还没有评论」。
- 举报弹窗：选择举报原因（虚假信息/违规内容/恶意行为/其他）+ 补充说明。
- 申请认领弹窗：认领说明（必填，`maxlength=300`）+ 证明材料图片上传（`el-upload` picture-card）。仅 `student` 且物品为招领中/待认领时可提交。
- 收藏/点赞逻辑：`favoriteItemIds`、`likedItemIds` 存于 Pinia，在详情页即时切换并控制状态与计数。

### 3.5 个人中心

- **我的发布**：只展示当前用户作为作者的记录，含地点、日期、状态，可查看详情。
- **我的认领**：展示当前用户提交的认领申请、物品、申请时间和审核状态。

### 3.6 管理员功能

#### 审核中心（AuditCenter）

- 待处理指标卡、本周已处理、当前筛选结果；
- 待审核列表：图片、物品名称、分类（标签）、发布者、发布时间、状态；
- 搜索（名称/发布者/地点）+ 状态 + 类型筛选 + 分页；
- 操作：「通过」（状态 → 招领中）；「驳回」需填写原因（原因必填校验）。

#### 物品管理（ManageItems）

- 全部物品表格：缩略图、物品名称、标签、发布者、地点、状态、发布时间；
- 搜索 + 类型 + 状态筛选（招领中 / 待认领 / 已认领 / 已关闭）+ 分页；
- 操作：「编辑」（弹窗改名称/标签/地点/描述）、「下架/上架」（`ElMessageBox` 二次确认）、「删除」（`ElMessageBox` 确认）。

### 3.7 系统管理员

- 数据总览：指标卡（累计发布/成功归还/待处理/归还率）、近 30 日趋势（CSS 占位）、高频地点排名（CSS 占位）。
- 账号管理：使用 `el-table` 展示所有注册用户（账号、昵称、角色、状态）。
  - **修改角色**：弹窗内通过角色下拉框（普通学生 / 失物招领管理员 / 系统管理员）为用户分配新角色，提交后更新 Pinia 并持久化；
  - **禁用/启用**：模拟账号封禁，被禁用账号无法再登录（系统内置管理员除外）。
- 公告管理：公告列表（标题、日期、标签）与编辑入口。

## 4. 非功能要求

- 页面适配桌面端和移动端（存在移动端断点样式）。
- 主要操作应有即时反馈（toast / `ElMessage`）。
- 组件和状态命名保持清晰，便于初学者理解维护。
- 应避免未经授权的角色操作；真实版本必须由后端再次校验权限。
- 生产构建必须通过 `npm run type-check` 与 `npm run build`。

## 5. 权限与路由守卫

### 5.1 路由分类

- `/login`：公开登录页，`guestOnly`；已登录访问时跳回当前角色首页。
- `/register`：公开注册页，`guestOnly`；未登录即可访问（免登录公开路由）。
- `/app`：入口，登录后跳转到当前角色首页。
- `/app/home`、`/app/publish`、`/app/posts`、`/app/claims`：仅 `student`。
- `/app/audit`、`/app/manage`：仅 `itemAdmin`。
- `/app/dashboard`、`/app/users`、`/app/notices`：仅 `systemAdmin`。
- 未登录访问受保护路由 → `/login?redirect=<原地址>`。
- 已登录但角色不在 `meta.roles` 中 → 提示「无权限访问」并返回当前角色首页。
- 未匹配路由 → 重定向到 `/app`，再由入口分流到当前角色首页。

### 5.2 双层权限控制

1. **菜单层**：`src/navigation.ts` 按角色返回菜单，`App.vue` 的 `visibleNavItems` 仅渲染当前角色菜单。
2. **路由层**：路由守卫 `src/router.ts` 的 `beforeEach` 校验 `meta.roles`，拦截直接输入地址等越权行为。
### 5.3 角色权限矩阵

符号说明：`✓` 允许，`—` 当前角色不可见且不可访问。

| 菜单/路由 | 学生 | 失物招领管理员 | 系统管理员 |
| --- | ---: | ---: | ---: |
| 发现物品 `/app/home` | ✓ | — | — |
| 发布信息 `/app/publish` | ✓ | — | — |
| 我的发布 `/app/posts` | ✓ | — | — |
| 我的认领 `/app/claims` | ✓ | — | — |
| 审核中心 `/app/audit` | — | ✓ | — |
| 物品管理 `/app/manage` | — | ✓ | — |
| 数据总览 `/app/dashboard` | — | — | ✓ |
| 账号管理 `/app/users` | — | — | ✓ |
| 公告管理 `/app/notices` | — | — | ✓ |

| 能力 | 学生 | 失物招领管理员 | 系统管理员 |
| --- | ---: | ---: | ---: |
| 搜索和查看物品 | ✓ | — | — |
| 创建失物/招领信息 | ✓ | — | — |
| 查看自己的发布和认领 | ✓ | — | — |
| 申请认领（仅招领中/待认领） | ✓ | — | — |
| 点赞/收藏/评论 | ✓ | — | — |
| 审核发布信息（通过/驳回） | — | ✓ | — |
| 编辑/下架/上架/删除物品 | — | ✓ | — |
| 查看全校数据总览 | — | — | ✓ |
| 管理账号 | — | — | ✓ |
| 管理公告 | — | — | ✓ |

### 5.4 RBAC 业务限制

- 只有 `student` 可发布信息、提交认领申请、评论。
- 只有 `itemAdmin` 可审核、驳回、编辑、下架/上架、删除物品。
- 只有 `systemAdmin` 可查看数据总览、账号管理、公告。
- **认领业务限制**：已认领 / 已关闭 / 已驳回 / 待审核状态的物品不可再申请认领。

> 前端 RBAC 仅用于体验；真实后端必须再次校验 Token、身份与数据操作权限（后端 API 联调阶段实现）。

## 6. 建议的数据模型

### User

`id`、`name`、`studentNo`、`role`、`status`、`createdAt`

### Item

`id`、`type`、`title`、`tags`、`location`、`description`、`images`、`contact`、`status`、`authorId`、`createdAt`

### Claim

`id`、`itemId`、`applicantId`、`description`、`proof`、`status`、`reviewerId`、`createdAt`、`updatedAt`

### Notice

`id`、`title`、`content`、`tag`、`publishedBy`、`publishedAt`、`status`

### Comment

`id`、`itemId`、`author`、`avatar`、`date`、`content`、`likes`、`liked`、`parentId`、`replyTo`

## 7. 用户认证与权限

### 7.1 注册逻辑

- 注册页开放 **普通学生** 注册，页面仅提供账号、密码、确认密码、真实姓名、联系方式字段，**不提供角色选择**。
- 新注册用户 `role` 默认写死为 `student`（普通学生）。
- 注册成功后数据写入 Pinia（`registeredUsers`）并持久化到 `localStorage.registered_users`，弹出成功提示后跳转登录页。
- 注册校验：字段必填、密码至少 6 位、两次密码一致、账号未占用；`sysadmin` 为系统保留账号，不可被注册。

### 7.2 登录逻辑

- 登录页**去除身份下拉框**，仅保留账号 + 密码输入。
- 登录时从 Pinia / `localStorage` 的用户列表中进行**真实账号密码匹配**（`authenticate`），匹配成功才登录：
  - 账号或密码不匹配 → 提示「账号或密码错误」；
  - 账号已禁用 → 提示「该账号已被禁用」，拒绝登录。
- 登录成功后写入 `localStorage.auth_token = mock-token`、`localStorage.role`、`localStorage.account`，设置 `isAuthenticated = true`。
- 通过**路由守卫**根据登录用户的 `role` 跳转到对应工作台首页：
  - `student` → `/app/home`；
  - `itemAdmin` → `/app/audit`；
  - `systemAdmin` → `/app/dashboard`。
- 退出登录：清除 Mock Token、角色与账号。

### 7.3 权限分配闭环（RBAC）

- 角色分配由 **系统管理员** 在【账号管理】页面完成：
  - **修改角色**：可将普通学生提升为失物招领管理员或系统管理员（或降级），提交后更新 Pinia 并持久化，立即生效；
  - **禁用/启用**：模拟账号封禁，被禁用账号无法再登录；启用后恢复登录。
- 新注册用户均为普通学生，需由系统管理员提权后方可使用管理员工作台，形成「注册 → 按需提权 → 分级访问 → 封禁」的完整闭环。
- 系统内置的 `sysadmin` 账号**不可被修改角色、不可被禁用**。

### 7.4 内置账号

- 系统初始最高权限账号写死为 **`sysadmin / 123456`**（角色 `systemAdmin`，昵称「系统管理员」），由 Pinia 初始数据注入，并确保其始终存在且不可被前端注册或修改。

### 7.5 刷新与持久化

- 用户列表持久化到 `localStorage.registered_users`，刷新后恢复；账号、角色、认证状态同样从本地恢复。
- 正式版本应由 Go 后端校验账号密码并返回 JWT / Token，前端 RBAC 仅用于演示。

## 8. TypeScript 实现要求

- 入口、路由、Pinia store、Vite 配置均使用 TypeScript。
- 定义 `Role`、`ItemType`、`ItemStatus`、`User`、`Item`、`Claim`、`Comment` 等类型。
- 使用 `vue-tsc --noEmit` 进行类型检查。

## 9. 当前边界与后续接入点

- 数据在 Pinia 内存中，刷新后恢复 Mock。
- 图片本地预览，未上传服务器。
- 认领申请的管理员同意/拒绝/完成归还操作未接入。
- 后端 API（Go + MySQL）与真实 Token/JWT 未接入。
- ECharts 数据大屏、移动端适配、Vercel 部署为后续里程碑。