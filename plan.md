# 拾光校园失物招领系统开发计划

## 1. 总体路线

采用「先完成可演示前端，再接入真实服务」的路线。当前前端已经完成 **TypeScript 重构、登录流程、路由守卫与角色级 RBAC、学生端核心业务闭环、管理员物品管理与审核中心**。下一步聚焦**系统管理员数据大屏**、**移动端适配**、**Vercel 部署上线**以及**后端 API 联调准备**，最终接入 Go + MySQL 真实后端与 JWT 认证。

## 2. 已完成阶段

### 阶段一：前端工程初始化 ✅

- [x] 创建 Vue 3 + Vite 项目
- [x] 配置 npm scripts：`dev`、`build`、`preview`、`type-check`
- [x] 安装 Vue、Pinia、Vue Router、Element Plus、ECharts
- [x] 增加 `vite.config.ts` 并注册 Vue 插件

### 阶段二：核心状态与菜单 ✅

- [x] 建立 Pinia `app` store
- [x] 建立学生、失物招领管理员、系统管理员三种角色
- [x] 建立物品、公告、认领申请、评论 Mock 数据
- [x] 建立角色对应菜单配置（`src/navigation.ts`）
- [x] 实现发布、认领、审核、收藏、点赞、评论等 actions

### 阶段三：核心页面交互 ✅

- [x] 学生首页、搜索、多维筛选和详情弹窗
- [x] 发布失物或招领信息表单
- [x] 我的发布和我的认领（个人中心）
- [x] 管理员审核中心和物品管理
- [x] 系统管理员数据总览、账号管理和公告管理
- [x] 响应式布局和操作提示

### 阶段四：质量验证 ✅

- [x] 修复 `App.vue` 脚本解析问题
- [x] 修复 Vite Vue 插件配置问题
- [x] 通过 `npm run build` 生产构建

### 阶段五：TypeScript 重构 ✅

- [x] 入口文件迁移为 `src/main.ts`
- [x] 路由、Pinia store、Vite 配置迁移为 TypeScript
- [x] 增加 `Role`、`ItemType`、`ItemStatus`、`User`、`Item`、`Claim`、`Comment` 类型
- [x] 增加 `src/AppRoot.vue` 作为根渲染，挂载 `RouterView`
- [x] 增加 `vue-tsc --noEmit` 类型检查脚本

### 阶段六：登录守卫与 RBAC 权限控制 ✅

- [x] 增加 `/login` 登录页面（账号/密码/身份选择）
- [x] 登录状态与本地 Mock Token 持久化
- [x] 未登录跳转登录页的路由守卫
- [x] 工作台、学生、失物招领管理员、系统管理员路由角色元信息
- [x] 根据角色限制菜单可见范围与页面访问范围
- [x] 修复工作台入口 / 直接输入地址越权切换的漏洞
- [x] 更新登录认证、路由守卫与权限矩阵文档

### 阶段七：学生端核心业务闭环 ✅

- [x] 首页多维筛选（搜索/类型/分类/地点/时间）+ 分页
- [x] 信息发布表单：多标签多选、层级地点、图片上传
- [x] 详情弹窗：图片轮播、点赞收藏、评论区与楼中楼回复
- [x] 申请认领弹窗：认领说明 + 证明材料图片上传
- [x] 举报弹窗
- [x] 图片上传美化与限制（最多 3 张、仅图片格式、预览删除）
- [x] 表单校验完善（名称/标签/地点/联系方式/描述/图片数量）

### 阶段八：管理员物品管理与审核中心 ✅

- [x] 审核中心：待审核列表、搜索筛选分页、通过 / 驳回（原因必填）
- [x] 物品管理：全部物品表格、编辑、下架/上架、删除（二次确认）
- [x] 审核与物品管理独立组件（`AuditCenter.vue`、`ManageItems.vue`）
- [x] 清理重复模块，修复地点选择器与表单布局重叠

### 阶段八·补充：动态地点选择器 ✅

- [x] 封装独立组件 `LocationSelector.vue`，不在 `App.vue` 中内联地点逻辑
- [x] 层级逻辑：校区 → 建筑/地点 →（按类型动态）楼栋 / 楼层 / 详细地址
- [x] 动态规则：宿舍楼需选择楼栋 + 楼层；教学楼/食堂/图书馆只需选择楼层；精弘桥/操场等平面地标直接显示详细地址文本框
- [x] 自动拼接完整地址（校区 · 建筑 · 楼栋 · 楼层 · 详细地址）
- [x] 动态出现的必填字段（楼栋/楼层/详细地址）均触发表单校验，未填不可提交
- [x] 输入框/下拉框视觉对齐、宽度自适应

### 阶段八·补充：用户认证体系与角色分配闭环 ✅

- [x] 登录页**去除身份下拉框**，改为真实账号密码匹配登录
- [x] 新增注册页 `Register.vue`：仅开放普通学生注册，默认 `role = student`
- [x] 登录按用户 `role` 由路由守卫跳转到对应工作台首页
- [x] 内置 `sysadmin / 123456` 系统初始最高权限账号（不可注册/修改/禁用）
- [x] 系统管理员在【账号管理】通过**修改角色**完成角色分配与提权（RBAC 闭环）
- [x] 系统管理员支持**禁用/启用**账号（模拟封禁）
- [x] 用户数据持久化到 `localStorage`（`registered_users`），刷新后恢复

## 3. 后续待办

> 以下为下一阶段迭代计划：**数据大屏（极简版）**、**移动端适配**、**Vercel 部署上线**。

### 阶段九：系统管理员数据大屏（极简版）

- [ ] 使用 ECharts 接入数据大屏（极简版），替换当前 CSS 占位图表
- [ ] 展示发布量、归还量、待处理量、归还率等指标
- [ ] 近 N 日趋势图、热门分类、高发地点、归还率走势
- [ ] 数据大屏响应式与读取体验优化

### 阶段十：移动端适配

- [ ] 完善移动端断点，适配侧边栏 / 顶部栏与内容区
- [ ] 表单、表格、弹窗在窄屏下的布局优化
- [ ] 详情弹窗在移动端的滚动与交互优化
- [ ] 验证三种角色在移动端的完整操作闭环

### 阶段十一：Vercel 部署上线

- [ ] 配置静态资源相对路径与 SPA fallback（`vercel.json`）
- [ ] 通过 `npm run build` 生成产物并本地预览
- [ ] 连接 Vercel 仓库，配置构建、输出目录与环境变量
- [ ] 部署预览并验证登录、RBAC 与单页路由

### 远期规划（不在本轮迭代内）：后端 API 联调准备

- [ ] 将 `App.vue` 业务区域拆分为更独立的页面组件，准备后端联调
- [ ] 设计 Go 后端分层结构与 REST API（User/Item/Claim/Notice/Comment）
- [ ] 设计核心数据表结构
- [ ] 实现登录、注册、JWT/Token 刷新与退出登录
- [ ] 实现物品增删改查、审核、认领申请与状态流转接口
- [ ] 实现账号、公告、统计与图片上传接口
- [ ] 前端将 Pinia actions 替换为 Axios API 请求（携带 Token）
- [ ] 联系方式脱敏、参数校验、错误处理、重复认领拦截与操作日志
- [ ] 补充 RBAC 路由守卫与核心流程自动化测试
- [ ] 编写 API 接口文档与部署说明

## 4. 里程碑

| 里程碑 | 交付结果 | 完成状态 |
| --- | --- | --- |
| M1 | Vue 项目可运行，核心依赖安装完成 | ✅ 已完成 |
| M2 | 三角色工作台和主要页面完成 | ✅ 已完成 |
| M3 | 发布、审核、认领闭环可演示 | ✅ 已完成 |
| M4 | TypeScript 重构、登录与前端 RBAC 完成 | ✅ 已完成 |
| M5 | 学生端核心业务闭环（图片/多标签/层级地点/详情交互） | ✅ 已完成 |
| M6 | 管理员物品管理与审核中心完成 | ✅ 已完成 |
| M6.5 | 动态地点联动选择器（校区→建筑→动态楼栋/楼层/地址） | ✅ 已完成 |
| M6.6 | 用户认证体系与角色分配闭环（注册、账号密码登录、系统管理员分配角色/禁用） | ✅ 已完成 |
| M7 | 系统管理员数据大屏（极简版）接入 ECharts | ⏳ 待完成 |
| M8 | 移动端适配 | ⏳ 待完成 |
| M9 | Vercel 部署上线 | ⏳ 待完成 |
| M10 | Go 后端、数据库与真实认证联调 | ⏳ 待完成 |